{
  "name": "workverse-jobportal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "seed": "npm --prefix backend run seed"
  },
  "keywords": [
    "job-portal",
    "neo4j",
    "react",
    "express",
    "graph-database"
  ],
  "author": "Kumar Saurav",
  "license": "ISC",
  "description": "WorkVerse is a full-stack job portal that uses React, Express, and Neo4j to connect candidates, employers, jobs, and skills through a graph-powered hiring workflow."
}
